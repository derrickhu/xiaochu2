# xiaochu2 技术架构设计

> 引擎：PixiJS 7 + TypeScript + Vite，复用 game2D_huahua 已验证的微信/抖音真机适配层。

## 1. 为什么换引擎

xiao_chu 用原生 Canvas 2D：

- 战斗场景每帧全屏 `clearRect` + 全量重绘，CPU 绘制是瓶颈
- 棋盘动画每帧 `createLinearGradient`，对象分配压力大
- 靠 `battleMemoryGuard` 限流粒子/飘字只是兜底

PixiJS 7 走 WebGL：Sprite 场景图 + GPU 合批，珠子/特效/飘字是常驻显示对象，
每帧只改 transform 不重绘，从根上解决卡顿。

## 2. 技术选型

| 项 | 选择 | 理由 |
|---|---|---|
| 渲染 | PixiJS 7.3.x（不用 v8） | v8 的 unsafe-eval / OffscreenCanvas / DOMAdapter 在小游戏环境问题多，v7 有 huahua 真机验证 |
| 语言 | TypeScript（strict） | 数值表 / 公式签名类型约束 |
| 构建 | Vite 6 + IIFE 单文件输出 | 复用 huahua 配置：`@pixi/*` dedupe + unsafe-eval 后处理插件 |
| 补间 | 自研 TweenManager（移植） | 零依赖，宠物攻击动效够用 |
| 骨骼动画 | 暂不引入 spine | 首版用 Sprite 位移 + AnimatedSprite 帧动画，留接口 |

## 3. 启动链路

```
build/<platform>/game.js          # 拷贝自 minigame/game.js
  → require('./runtime.js')       # 认 tt / tap / wx
  → require('./pixi-adapter/index')
  → require('./game-bundle.js')   # 来自 .bundle/ 或 .bundle-taptap/
      main.ts 第一行 import pixiUnsafeEvalPatch
      Game.init(GameGlobal.canvas)
      TextureCache.preload(...)
      SceneManager.switchTo('title')
```

开发者工具打开 `build/wechat/` 或 `build/douyin/`，不要打开 `minigame/`。组装说明见 [04-多平台构建.md](04-多平台构建.md)。

## 4. 目录结构

```
xiaochu2/
├── docs/
├── minigame/              # 共享内容树（不是工具打开目录）
│   ├── game.js / runtime.js / pixi-adapter/
│   └── images/ + subpackages/
├── platform/              # 各端 game.json、project.config.json
├── .bundle/               # Vite：微信/抖音 bundle
├── build/wechat|douyin|taptap/   # 工具打开这里
├── src/
│   ├── main.ts            # 入口
│   ├── core/              # 引擎层（与业务解耦）
│   │   ├── pixiUnsafeEvalPatch.ts
│   │   ├── Game.ts        # Application 单例 / 分辨率 / Ticker
│   │   ├── SceneManager.ts
│   │   ├── OverlayManager.ts
│   │   ├── TweenManager.ts
│   │   ├── EventBus.ts
│   │   ├── PlatformService.ts  # wx/tt/tap 宿主抽象
│   │   ├── TextureCache.ts     # 纹理加载缓存（分包/CDN 预留）
│   │   └── ObjectPool.ts       # 通用对象池（珠子/飘字/粒子）
│   ├── scenes/            # TitleScene / BattleScene
│   ├── game/              # 玩法逻辑（board / battle / pet）
│   ├── balance/           # 数值层（纯数据）
│   ├── formulas/          # 公式层（纯函数 + vitest 单测）
│   └── config/            # 资源映射表（IMAGE_MAP）
├── vite.config.ts / tsconfig.json / package.json
```

## 5. 渲染分层

```
Game.stage (sortableChildren)
├── Scene.container
│   ├── 背景层
│   ├── 敌人层（受击动效）
│   ├── 棋盘层（_cellsLayer 格底 / _beadsLayer 珠子，珠子走对象池）
│   ├── 队伍栏层（宠物头像，攻击时冲刺位移）
│   └── 特效层（ParticleContainer 粒子 + 飘字池）
└── OverlayManager.container (zIndex 10000, 全局弹窗)
```

## 6. 性能策略（转珠场景专项）

| 手段 | 说明 |
|---|---|
| 珠子对象池 | 36 颗珠 + 消除/下落复用 Sprite，不销毁重建 |
| ParticleContainer | 消除爆裂粒子，按机型分 lite/full 两档 |
| 纹理图集 | 珠子/战斗 UI 打 Spritesheet，减少 draw call 与解码内存 |
| 降帧 | 浏览器：`Game.setMaxFPS`；**小游戏真机禁止 Pixi maxFPS**（见踩坑记录） |
| 纹理缓存 | TextureCache Map 缓存 + inflight 去重 + 失败重试 |
| 内存 | 场景切换 `destroy({children:true})`，Tween 目标销毁检测 |

## 7. 多平台

- `minigame/runtime.js` + `PlatformService.ts`：认 `tt` / `tap` / `wx`，业务禁止直接写宿主全局
- `platform/<端>/`：只有 json（Tap 另打 `VITE_PLATFORM=taptap` bundle）
- `build/<端>/`：开发者工具打开的目录
- 存档前缀见 `gameKeyScope.ts`；完整流程见 [04-多平台构建.md](04-多平台构建.md)
- 技术点索引：[05-关键技术点.md](05-关键技术点.md)

## 8. 美术资源复用

直接复用 xiao_chu 的资源（~673 张）：

| 来源 | 数量 | 用途 |
|---|---|---|
| `assets/orbs/` | 6 | 转珠棋子（首版已拷贝） |
| `assets/pets/` | 200 | 灵宠立绘 / 头像 |
| `assets/enemies/` | 221 | 敌人立绘 |
| `assets/ui/` | 124 | UI 控件 |

首版主包只放珠子 + 必要 UI，宠物/敌人立绘走分包或 CDN 懒加载。
