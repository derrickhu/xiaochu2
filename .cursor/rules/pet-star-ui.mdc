---
description: 宠物星级 UI 全项目统一（战斗同款 Q 版星贴图）
alwaysApply: true
---

# 宠物星级 UI（强制统一）

凡展示**灵宠星级**（1~5★），必须与战斗界面同一套视觉，禁止另起文字星、自绘星、独立组件。

## 单一真源

| 项 | 路径 |
|---|---|
| 贴图 | `UI_BATTLE_IMAGES.petStar` → `battle_pet_star.png` |
| 组件 | `makeStarRow`（`src/ui/GrowthVisual.ts`），**默认 `style: 'sprite'`** |
| 等级+星一行 | `makeLevelStarLine`（内部也走 `makeStarRow` sprite） |

## 必做

1. 战斗栏、编队上阵槽、可选列表、图鉴卡、详情页、结算等：**一律** `makeStarRow` / `makeLevelStarLine`。
2. 需要预加载的页面把 `UI_BATTLE_IMAGES.petStar` 加进对应 `*_SHELL_IMAGES`。
3. 尺寸只调 `starSize` / `gap`，不换素材、不换点亮/暗星逻辑。

## 禁止

- 手写 `'★'.repeat(n)` / `☆` 表示宠物星级
- `style: 'glyph'` 用于正式 UI（仅无贴图兜底/调试）
- 新建第二套星星图标或局部 Graphics 画星

## 关卡通关星（1~3）

若地图/结算要显示通关星，**复用** `makeStarRow({ maxStar: 3, style: 'sprite' })`，勿另造样式。
