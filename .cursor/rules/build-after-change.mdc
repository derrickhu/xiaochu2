---
description: xiaochu2 代码改动后必须重新构建，确保 minigame/game-bundle.js 与源码同步
alwaysApply: true
---

# xiaochu2 改动后重新构建

在 `xiaochu2/` 内完成任何**源码、资源或配置**改动后，Agent **必须在本轮对话内实际执行**构建，不得只口头说「请自行 build」或留到用户提醒。

## 必做命令

```bash
cd xiaochu2 && npm run build
```

- 微信/抖音小游戏运行的是 `minigame/game-bundle.js`，不构建则开发者工具里看不到最新改动。
- 构建脚本会顺带执行 `scripts/organize-subpackages.mjs`，改分包资源后同样依赖此命令。
- **构建失败须先修复再继续**，不得向用户报告「已完成」并留下 broken bundle。

## 额外验证

| 改动类型 | 额外命令 |
|---------|---------|
| 公式 / 存档 / 战斗逻辑 | `npm test` |
| 类型 / 模块拆分 | `npm run typecheck` |

## 完成标准

向用户汇报任务完成前，确认以下之一：

1. 已执行 `npm run build` 且 exit code 0；或
2. 本轮仅回答问题、未改任何 `xiaochu2/` 文件（纯阅读/解释可跳过构建）。

若因环境原因无法构建，必须在回复中明确说明，不得默认用户已构建。
