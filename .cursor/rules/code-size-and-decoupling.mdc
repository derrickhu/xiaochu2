---
description: xiaochu2 代码体量与解耦约束，避免单文件/单函数持续膨胀
alwaysApply: true
---

# xiaochu2 代码体量与解耦硬规则

这是项目与 Agent 约定层面的硬规则，不是 ESLint/CI 机械拦截。改动代码时必须主动遵守；后续如需自动失败，再单独引入 lint/CI。

## 体量上限（是信号，不是机械红线）

- 400 行 / 80 行是**触发审视的信号**，不是「到点就切一刀」的红线。超标只意味着「该评估是否存在多职责」，不意味着可以为压行数而硬拆。
- 单函数/方法 ~80 行、参数 ≤5、嵌套 ≤4 仍是默认目标；个别内聚的演出/状态机略微超出、且强行拆分会损害可读性时可以保留，但要在评审中说明理由。
- 类实例字段超过约 15 个时，视为 god-object 信号，应优先拆协作者、上下文对象或纯 helper。
- 一个**真正内聚的单元**（编排者 Scene、单一状态机、单一算法）即使略超 400 行，也优于把它机械切成互相穿透 `this` 的碎片——后者更糟。

## 拆分必须基于职责解耦（禁止机械拆分）

- **严禁按行数机械切割**：不得把一个类/文件按「每段 ~300 行」切成 `xxxMigrated1/2/3`、`xxxPart1/2`、`xxxA/B/C` 这类无语义命名的碎片。拆分边界必须落在职责接缝上。
- 每个拆出的文件名必须**自解释其职责**（如 `BattleHud`、`BattlePetBar`、`BattleFx`、`BattleResultOverlay`），让人一看就知道里面是什么。
- **严禁用 `@ts-nocheck` / `any` 换取拆分**。若拆分后类型不通过，说明边界没切对或共享状态没理清，必须修到类型通过为止。
- **严禁把方法体平移成「接收整个 `this`/scene 的自由函数」**只为缩小原文件——那不是解耦，只是把隐式耦合搬了个地方，还会绕过类型检查、埋下运行时 bug。
- 正确做法：抽出**拥有自身状态、暴露语义化方法**的协作类/模块；跨组件协作通过显式参数、上下文对象或最小回调接口注入。

## 解耦原则

- 视图、输入、动画、数据、持久化分层；不要把多类职责堆进同一个 Scene/Controller。
- `balance/*` 优先保持纯数据与纯工厂，避免混入运行时状态。
- 跨模块共享状态必须通过显式参数、上下文对象或最小接口注入，禁止依赖巨型 `this` 隐式读写。
- 重构时「不改行为」指**外部可观察行为**（表现/时序/数值）保持一致，**不是**逐字平移内部实现；可以为了正确的职责边界调整内部结构，但必须保持类型通过且行为等价。

## 豁免

- `**/__tests__/**`、`*.test.ts`、`*.snap`、自动生成文件不计入硬上限。
- 纯数据表可以豁免，但必须无复杂逻辑、按段注释清晰、每段可搜索，并在文件头标注：

```ts
// @size-exempt: 纯数据表，按章节/类型分段维护
```

## 验证

- 任意源码、资源或配置改动后仍需遵守 `build-after-change.mdc`：任务结束前执行 `npm run build`。
- 涉及拆分、类型导出、公式或存档逻辑时，同步执行 `npm run typecheck` 与 `npm test`。
